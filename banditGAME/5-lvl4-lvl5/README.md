## INSTRUCTIONS;

The password for the next level is stored in the only human-readable file in the inhere directory. Tip: if your terminal is messed up, try the “reset” command.

## SOLUTION;

several files inside 'inhere' repo, had to test them all individually with

    $ cat < <filename>

## VULNERABILITIES;

dont hide your sensible files inside of a repo where there's a bunch of non-human-readable files